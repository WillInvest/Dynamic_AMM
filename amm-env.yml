name: amm-env
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - numpy
  - scipy
  - polars
  - pandas
  - matplotlib
  - jupyter
  - tqdm
  - pip
  - pip:
    - tqdm
    - polars
    - pyarrow  # for parquet support 
    - numba